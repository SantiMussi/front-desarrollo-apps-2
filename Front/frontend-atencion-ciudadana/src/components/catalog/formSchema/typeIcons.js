import { AlignLeft, Calendar, Hash, List, ToggleLeft, Type } from "lucide-react";

export const TYPE_ICONS = {
  TEXT: Type,
  TEXTAREA: AlignLeft,
  SELECT: List,
  BOOLEAN: ToggleLeft,
  NUMBER: Hash,
  DATE: Calendar,
};
