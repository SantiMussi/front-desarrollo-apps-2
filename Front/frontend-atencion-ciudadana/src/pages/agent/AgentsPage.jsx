import { useMemo, useState } from "react";
import { Check, ChevronDown, Mail, MoreHorizontal, Search, UserPlus, Users, X } from "lucide-react";
import { AGENT_STATUS, MOCK_AGENTS } from "../../data/mockAgents";

function StatusBadge({ status }) {
  const config = AGENT_STATUS[status];
  return <span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-semibold ring-1 ring-inset ${config.badge}`}><i className={`h-1.5 w-1.5 rounded-full ${config.dot}`} />{config.label}</span>;
}

function InviteDialog({ onClose, onInvite }) {
  const [form, setForm] = useState({ email: "", area: "Atención General" });
  return <div className="fixed inset-0 z-[60] grid place-items-center bg-slate-950/40 p-4 backdrop-blur-sm" onMouseDown={(event) => event.target === event.currentTarget && onClose()}>
    <form onSubmit={(event) => { event.preventDefault(); onInvite(form); }} className="w-full max-w-md rounded-2xl bg-white shadow-2xl">
      <div className="flex items-start justify-between border-b border-slate-100 p-5"><div><h2 className="text-lg font-bold text-slate-900">Invitar agente</h2><p className="mt-1 text-sm text-slate-500">Recibirá un correo para configurar su cuenta.</p></div><button type="button" onClick={onClose} className="rounded-lg p-1.5 text-slate-400 hover:bg-slate-100" aria-label="Cerrar"><X className="h-5 w-5" /></button></div>
      <div className="space-y-4 p-5"><label className="block text-sm font-semibold text-slate-700">Correo electrónico<input required type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} placeholder="nombre@ciudaduade.gob.ar" className="mt-1.5 w-full rounded-lg border border-slate-300 px-3 py-2.5 font-normal outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100" /></label><label className="block text-sm font-semibold text-slate-700">Área asignada<select value={form.area} onChange={(event) => setForm({ ...form, area: event.target.value })} className="mt-1.5 w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 font-normal">{["Atención General", "Obras Públicas", "Higiene Urbana", "Espacios Verdes", "Tránsito", "Alumbrado Público"].map((area) => <option key={area}>{area}</option>)}</select></label></div>
      <div className="flex justify-end gap-2 rounded-b-2xl bg-slate-50 px-5 py-4"><button type="button" onClick={onClose} className="rounded-lg border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-700">Cancelar</button><button className="inline-flex items-center gap-2 rounded-lg bg-[#0F2C59] px-4 py-2 text-sm font-semibold text-white hover:bg-[#163d75]"><Mail className="h-4 w-4" /> Enviar invitación</button></div>
    </form>
  </div>;
}

export default function AgentsPage() {
  const [query, setQuery] = useState("");
  const [status, setStatus] = useState("");
  const [area, setArea] = useState("");
  const [inviteOpen, setInviteOpen] = useState(false);
  const [notice, setNotice] = useState("");
  const areas = [...new Set(MOCK_AGENTS.map((agent) => agent.area))];
  const agents = useMemo(() => MOCK_AGENTS.filter((agent) => {
    const text = `${agent.name} ${agent.email}`.toLowerCase();
    return text.includes(query.trim().toLowerCase()) && (!status || agent.status === status) && (!area || agent.area === area);
  }), [query, status, area]);
  const activeFilters = query || status || area;
  const available = MOCK_AGENTS.filter((agent) => agent.status === "available").length;
  const avgLoad = Math.round(MOCK_AGENTS.reduce((sum, agent) => sum + agent.activeTickets / agent.capacity, 0) / MOCK_AGENTS.length * 100);

  return <div className="h-full overflow-y-auto bg-[#f7f8fa]">
    <header className="border-b border-slate-200 bg-white px-5 py-5 md:px-8"><div className="mx-auto flex max-w-7xl flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="mb-1 flex items-center gap-2 text-xs font-bold uppercase tracking-widest text-blue-700"><Users className="h-3.5 w-3.5" /> Equipo de atención</p><h1 className="text-2xl font-bold tracking-tight text-slate-900">Agentes</h1><p className="mt-1 text-sm text-slate-500">Gestioná el equipo de atención y su carga de trabajo.</p></div><button onClick={() => setInviteOpen(true)} className="inline-flex w-fit items-center gap-2 rounded-lg bg-[#0F2C59] px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-[#163d75]"><UserPlus className="h-4 w-4" /> Invitar agente</button></div></header>
    <main className="mx-auto max-w-7xl space-y-5 px-5 py-6 md:px-8 md:py-8">
      {notice && <div className="flex items-center justify-between rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800"><span className="flex items-center gap-2"><Check className="h-4 w-4" />{notice}</span><button onClick={() => setNotice("")} aria-label="Cerrar aviso"><X className="h-4 w-4" /></button></div>}
      <section className="grid gap-4 sm:grid-cols-3"><div className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Equipo total</p><p className="mt-1 text-2xl font-bold text-slate-900">{MOCK_AGENTS.length}</p></div><div className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Disponibles ahora</p><p className="mt-1 text-2xl font-bold text-emerald-600">{available}</p></div><div className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">Carga promedio</p><p className="mt-1 text-2xl font-bold text-slate-900">{avgLoad}%</p></div></section>
      <section className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex flex-col gap-3 lg:flex-row lg:items-center"><label className="relative flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar por nombre o correo…" className="w-full rounded-lg border border-slate-300 py-2.5 pl-9 pr-3 text-sm outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100" /></label><div className="relative"><select value={status} onChange={(event) => setStatus(event.target.value)} className="w-full appearance-none rounded-lg border border-slate-300 bg-white py-2.5 pl-3 pr-9 text-sm font-medium text-slate-700 lg:w-44"><option value="">Todos los estados</option>{Object.entries(AGENT_STATUS).map(([key, value]) => <option key={key} value={key}>{value.label}</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /></div><div className="relative"><select value={area} onChange={(event) => setArea(event.target.value)} className="w-full appearance-none rounded-lg border border-slate-300 bg-white py-2.5 pl-3 pr-9 text-sm font-medium text-slate-700 lg:w-48"><option value="">Todas las áreas</option>{areas.map((item) => <option key={item}>{item}</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /></div>{activeFilters && <button onClick={() => { setQuery(""); setStatus(""); setArea(""); }} className="text-sm font-semibold text-red-600 hover:underline">Limpiar filtros</button>}</div></section>
      <section className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div><h2 className="font-bold text-slate-900">Directorio del equipo</h2><p className="mt-0.5 text-xs text-slate-500">{agents.length} {agents.length === 1 ? "agente encontrado" : "agentes encontrados"}</p></div></div><div className="overflow-x-auto"><table className="w-full min-w-[830px] text-left"><thead className="bg-slate-50 text-[11px] font-bold uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3">Agente</th><th className="px-4 py-3">Estado</th><th className="px-4 py-3">Área asignada</th><th className="px-4 py-3">Carga de trabajo</th><th className="px-4 py-3">SLA promedio</th><th className="px-4 py-3">Resueltos</th><th className="px-5 py-3 text-right">Acciones</th></tr></thead><tbody className="divide-y divide-slate-100">{agents.map((agent) => { const load = agent.activeTickets / agent.capacity * 100; return <tr key={agent.id} className="group hover:bg-slate-50/80"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className={`relative grid h-10 w-10 shrink-0 place-items-center rounded-full text-xs font-bold ${agent.color}`}>{agent.initials}<i className={`absolute bottom-0 right-0 h-3 w-3 rounded-full border-2 border-white ${AGENT_STATUS[agent.status].dot}`} /></span><div><p className="text-sm font-semibold text-slate-900">{agent.name}</p><p className="mt-0.5 text-xs text-slate-500">{agent.email}</p></div></div></td><td className="px-4 py-4"><StatusBadge status={agent.status} /></td><td className="px-4 py-4 text-sm font-medium text-slate-700">{agent.area}</td><td className="px-4 py-4"><div className="flex items-center gap-3"><span className="w-8 text-sm font-semibold text-slate-700">{agent.activeTickets}/{agent.capacity}</span><div className="h-2 w-24 overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${load >= 80 ? "bg-amber-500" : "bg-[#0F2C59]"}`} style={{ width: `${load}%` }} /></div></div></td><td className={`px-4 py-4 text-sm font-bold ${agent.sla >= 95 ? "text-emerald-600" : agent.sla >= 90 ? "text-amber-600" : "text-red-600"}`}>{agent.sla}%</td><td className="px-4 py-4 text-sm font-semibold text-slate-700">{agent.resolvedThisMonth}<span className="ml-1 font-normal text-slate-400">este mes</span></td><td className="px-5 py-4 text-right"><button className="rounded-lg p-2 text-slate-400 hover:bg-slate-200 hover:text-slate-700" title={`Acciones para ${agent.name}`}><MoreHorizontal className="h-5 w-5" /></button></td></tr>; })}</tbody></table></div>{agents.length === 0 && <div className="px-6 py-16 text-center"><span className="mx-auto mb-3 grid h-12 w-12 place-items-center rounded-full bg-slate-100 text-slate-400"><Search className="h-5 w-5" /></span><p className="font-semibold text-slate-800">No encontramos agentes</p><p className="mt-1 text-sm text-slate-500">Probá con otros términos o quitá algún filtro.</p></div>}</section>
    </main>
    {inviteOpen && <InviteDialog onClose={() => setInviteOpen(false)} onInvite={({ email }) => { setInviteOpen(false); setNotice(`Invitación enviada a ${email}.`); }} />}
  </div>;
}